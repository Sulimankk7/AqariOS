namespace PropertyOS.Domain.Companies.Enums;

/// <summary>
/// PostgreSQL enum: late_fee_type_enum
/// Labels (snake_case mapped by Npgsql): none, fixed, percentage
/// </summary>
public enum LateFeeType
{
    None,
    Fixed,
    Percentage
}
